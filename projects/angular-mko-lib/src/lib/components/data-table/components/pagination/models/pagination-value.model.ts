export interface PaginationValue {
    /**
     * The current page number.
     */
    page: number;

    /**
     * The number of items to display per page (pagination limit).
     */
    limit: number;
}